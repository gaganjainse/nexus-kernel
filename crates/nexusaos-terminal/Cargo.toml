[package]
name = "nexusaos-terminal"
version = "0.1.0"
license = "MIT"
edition = "2021"
description = "PTY process manager and terminal emulator bridge for NexusAOS"

[dependencies]
nexusaos-kernel.workspace = true
portable-pty.workspace = true
tokio.workspace = true
thiserror.workspace = true
tracing.workspace = true
vte = "0.13"

[dev-dependencies]
tempfile = "3"
