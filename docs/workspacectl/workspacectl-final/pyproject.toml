[project]
name = "workspacectl"
version = "0.1.0"
description = "Safe workspace/file organizer with planning, watching, and rollback."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "OpenAI", email = "noreply@openai.com"}]
dependencies = []

[project.scripts]
workspacectl = "workspacectl.cli:main"

[tool.ruff]
line-length = 100
